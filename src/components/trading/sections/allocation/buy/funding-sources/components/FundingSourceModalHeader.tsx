
import React from "react";
import { DialogHeader, DialogTitle } from "@/components/ui/dialog";

export const FundingSourceModalHeader: React.FC = () => {
  return (
    <DialogHeader>
      <DialogTitle>Select Funding Sources</DialogTitle>
    </DialogHeader>
  );
};
